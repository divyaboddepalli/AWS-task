# To run the application with a local PostgreSQL database, uncomment the following line
# and replace the placeholder with your actual database connection string.
# DATABASE_URL="postgresql://user:password@host:port/db"